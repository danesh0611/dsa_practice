import re
class Solution:
    def isPalindrome(self, s: str) -> bool:
        k=re.sub(r'[^a-zA-Z0-9]', '', s).lower()
        if k[::-1]==k:
            return True
        else:
            return False
        
